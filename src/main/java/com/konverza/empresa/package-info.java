/**
 * Company/tenant context — a single global record (name, free-text context)
 * for the whole install, not one row per company (add-users-empresa-profile).
 * Admin creates and edits it; Autoridad (EXEC) views it read-only.
 */
package com.konverza.empresa;
